<div class="modal fade" id="deleteModal" tabindex="-1" role="dialog" aria-labelledby="deleteModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="deleteModalLabel">Czy na pewno chcesz usunąć ten element?</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                Po usunięciu nie będzie możliwości cofnięcia akcji!
            </div>
            <div class="modal-footer">
                <button type="button" class="btn bg-gradient-secondary" data-bs-dismiss="modal">Anuluj</button>
                {% if app.request.attributes.get('_route') == 'app_task_show' %}
                    <a href="{{ path('app_task_delete', {'projectId' : app.request.attributes.get('projectId'), 'taskId' : app.request.attributes.get('taskId')}) }}" class="btn bg-gradient-danger">Usuń</a>
                {% else %}
                    <a href="{{ path('app_project_delete', {'projectId' : app.request.attributes.get('projectId')}) }}" class="btn bg-gradient-danger">Usuń</a>
                {% endif %}
            </div>
        </div>
    </div>
</div>