{% extends 'base.panel.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <h1 class="display-4">{{ title }}</h1>
    <hr/>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn bg-gradient-dark mt-4 mb-0">Dodaj projekt</button>
    {{ form_end(form) }}
{% endblock %}
